# Action

Action: Add an inbox analysis step before context export.

Result: The agent can inspect summaries, events, and candidate relations before
building a compact context pack.

Check: Applying a review item should use stable IDs and dry-run mode first.
